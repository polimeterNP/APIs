import React, { Component } from "react";

const rectangle = {
  height: "100%",
  width: "100%",
  color: "#1942bf"
};

export default class MediaLinks extends Component {
  render() {
    return <div style={rectangle} >VERIFIED MEDIA LINKS (INTERVIEWS)</div>;
  }
}
