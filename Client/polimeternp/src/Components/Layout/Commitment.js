import React, { Component } from "react";

const rectangle = {
  height: "100%",
  width: "100%",
  color: "#1942bf"

};

export default class Commitment extends Component {
  render() {
    return <div style={rectangle}>COMMITMENT TRACKING</div>;
  }
}
